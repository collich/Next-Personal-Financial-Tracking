const TableDataOut = ({ data }) => {
  // const renderData = data.map( datum => {

  // })
  console.log(data);

  return (
    // {renderData}
    <div>

    </div>
  )
}

export default TableDataOut